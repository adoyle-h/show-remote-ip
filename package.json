{
	"name": "show-remote-ip",
	"version": "1.1.0",
	"description": "A chrome extension that displays the server IP that is currently accessing, supports multi-language and floating window position switching. 显示当前访问网页的服务器 IP，支持多语言与浮窗位置切换。",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "ADoyle <adoyle.h@gmail.com>",
	"license": "APACHE-2.0",
	"devDependencies": {
		"@biomejs/biome": "2.1.2"
	}
}
